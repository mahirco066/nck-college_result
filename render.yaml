services:
  - type: web
    name: nkc-results-system
    runtime: node
    plan: free
    buildCommand: ""
    startCommand: node backend/src/server.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
